<?php
namespace Hagane;

class Config {
	function getConf() {
		return
			array(
				'appPath' => '../app/',
				'template' => 'main',
				// 'db_engine' => 'mysql',
				// 'db_server' => 'localhost',
				// 'db_database' => 'haganeso_crm',
				// 'db_user' => 'haganeso_dev',
				// 'db_password' => 'Bicarbonato1!',
				//'session_time' => 3600,
				'document_root' => '/Hagane/sisse/'
			);
	}

	function getModules() {
		return
			array();
	}

	function getRoutes() {
		// Add custom routes here so you can call them with a simple route name
		// Use the key of the element in the array as
		return
			array(
				'logout' => 'User/logout'
			);
	}
}
?>
